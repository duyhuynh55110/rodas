// Import Swiper components
import { Swiper, SwiperSlide } from 'swiper/vue';
import { TitleBar } from '@components';

export default {
    name: "CategoriesArea",
    components: {
        Swiper,
        SwiperSlide,
        TitleBar,
    },
}
